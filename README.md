# multi-file-opener-batch-creator
The most useless thing ever
it only works on windows because its a batch file generator
don't use this it's useless
its a python script that generates a batch file that opens multiple files at once
